# Changelog

## 0.1.0

Initial release.

- `SpectralEcto.JSONB`, an `Ecto.ParameterizedType` parameterized by `:module` and `:type`.
- `SpectralEcto.LoadError`, raised when a stored document does not match the declared type.
