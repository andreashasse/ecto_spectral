# Changelog

## 0.1.0

Initial release.

- `EctoSpectral.JSONB`, an `Ecto.ParameterizedType` parameterized by `:module` and `:type`.
- `EctoSpectral.LoadError`, raised when a stored document does not match the declared type.
